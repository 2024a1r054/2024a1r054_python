# 6. Initialize a variable balance=1000. Perform operations using shorthand operators like +=, -=, *=, /= and print the updated balance
  after each operation.

  balance=1000
print("Initial balance:",balance)
balance += 500
print("After adding 500:",balance)
balance -= 200
print("After subtracting 200:",balance)
balance *= 2
print("After multiplying by 2:",balance)
balance /= 4
print("After dividing by 4:",balance)
