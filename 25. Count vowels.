1. WAP a python program to take a word and count the number of vowels a,e,i,o,r=u.

text=input("Enter a word: ")
vowels="aeiouAEIOU"
vowel_count=sum(1 for char in text if char in vowels)
print(vowel_count)
